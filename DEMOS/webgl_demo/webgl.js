class WebGLGame extends Posit92 {
	async init() {
    await super.init()
  }

  async afterinit() {
    await super.afterinit()
  }

  cleanup() { super.cleanup() }
  update() { super.update() }
  draw() { super.draw() }
}
