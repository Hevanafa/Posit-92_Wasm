unit RichText;

interface

procedure TestRichTextLabel(
  const text: string;
  const x, y: integer;
  const colorTable: array of longword);


implementation

procedure TestRichTextLabel(
  const text: string;
  const x, y: integer;
  const colorTable: array of longword);
var
  bold, italic: boolean;
  currentColour: cardinal;
begin
  
end;

end.
